version: '3.8'

services:
  ainotate-ssh:
    build:
      context: ../../..
      dockerfile: tests/manual/ssh/Dockerfile
    ports:
      - "2222:22"      # SSH
      - "19432:19432"  # Ainotate default SSH port
    stdin_open: true
    tty: true
