{
  "name": "@ainotate/opencode",
  "version": "0.23.1",
  "type": "module",
  "description": "Ainotate plugin for OpenCode - interactive plan review with visual annotation",
  "author": "backnotprop",
  "license": "MIT OR Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/backnotprop/ainotate.git",
    "directory": "apps/opencode-plugin"
  },
  "homepage": "https://github.com/backnotprop/ainotate",
  "bugs": {
    "url": "https://github.com/backnotprop/ainotate/issues"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "ainotate",
    "plan-review",
    "ai-agent",
    "coding-agent"
  ],
  "main": "dist/index.js",
  "files": [
    "dist",
    "commands",
    "README.md",
    "ainotate.html",
    "review-editor.html"
  ],
  "scripts": {
    "build": "mkdir -p dist && cp ../hook/dist/index.html ./ainotate.html && cp ../review/dist/index.html ./review-editor.html && bun build embedded.ts --outfile dist/embedded.js --target bun --external @opencode-ai/plugin && bun build index.ts --outfile dist/index.js --target node",
    "postinstall": "mkdir -p ${XDG_CONFIG_HOME:-$HOME/.config}/opencode/commands && cp ./commands/*.md ${XDG_CONFIG_HOME:-$HOME/.config}/opencode/commands/ 2>/dev/null || true",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.1.10"
  },
  "devDependencies": {
    "@ainotate/server": "workspace:*",
    "@ainotate/shared": "workspace:*"
  },
  "peerDependencies": {
    "bun": ">=1.0.0"
  }
}
