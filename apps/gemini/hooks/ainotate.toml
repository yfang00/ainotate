# Ainotate policy for Gemini CLI
# Allows exit_plan_mode without TUI confirmation so the browser UI is the sole gate.
# Installed to: ~/.gemini/policies/ainotate.toml

[[rule]]
toolName = "exit_plan_mode"
decision = "allow"
priority = 100
